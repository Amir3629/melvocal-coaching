"use client"

import React from 'react';

// This is a placeholder component to prevent 'AIChat is not defined' errors
// Replace this with your actual implementation as needed
export const AIChat = ({ children, ...props }: React.PropsWithChildren<any>) => {
  return (
    <div className="hidden">
      {/* This component is intentionally empty as it's a placeholder */}
    </div>
  );
};

export default AIChat; 